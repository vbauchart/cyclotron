cyclotron:
  config:
    workdir: ./work/input
    archives: ./work/archive
  jobs:
    copy:
      source_dir: './tests/examples/import/'
      archive_dir: './tests/examples/archive/'
      file_pattern: 'filetoload_*.csv'
      import_cmd: 'cat %(filename)s'
    command:
      binary: ls
