cyclotron:
  directories:
    input: ./work/input
    archive: ./work/archive
    working: ./work/working
    reject: ./work/reject
  jobs:
    import:
      file_pattern: '{input}/filetoload_*.csv'
      command: 'ls {file_last}'
