cyclotron:
  directories:
    input: ./work/input
    archive: ./work/archive
    working: ./work/working
    reject: ./work/reject
  jobs:
    import:
      input_files: '${input}/filetoload_*.csv'
      command: 'ls {file_last}'
